# Pinned to what production runs. FastAPI 0.141 changed include_router to
# wrap routers instead of flattening them into app.routes, which the
# router-registration guard in app/main.py reads. The guard now handles
# both shapes, but an unpinned framework on a financial system is a
# surprise waiting for whoever next rebuilds the image.
fastapi==0.128.0
starlette==0.50.0
uvicorn[standard]
SQLAlchemy==2.0.21
asyncpg
alembic
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
cryptography
psycopg2-binary
pydantic[email]
python-dotenv
# zoneinfo needs an IANA database. The Debian base image ships one, but a
# Windows dev machine does not -- and without it every accounting date lookup
# (app.services.posting.business_date) raises rather than silently dating
# entries in UTC. Pinning the data here makes the behaviour the same everywhere.
tzdata
httpx
pytest
pytest-asyncio
reportlab
email-validator
openpyxl
python-multipart
pywebpush
py-vapid
